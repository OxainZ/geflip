org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.nonTransitiveRClass=true
# No AndroidX, no third-party libraries at all — the whole app is a WindowManager
# overlay hosting a WebView, and every API it needs shipped in Android 8.0.
