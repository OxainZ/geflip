name = "geflip-sync"
main = "src/worker.js"
compatibility_date = "2024-09-23"

# Workers KV binding. After `wrangler kv namespace create GEFLIP`, paste the printed
# id below (and the preview_id it prints for `wrangler dev`). See README.
[[kv_namespaces]]
binding = "GEFLIP"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
